examples/ : a folder that contains files of patterns, examples traces, and a linux script for generating traces according to the given patterns.


test-bin: a binary memory initialization file

test-hex: a hexadecimal memory initialization file  

RAM.vhd, tb_ram.vhd: an example showing how RAM is created and initialized in VHDL, and a test bench for testing it. 
